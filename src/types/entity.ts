/* ═══════════════════════════════════════════════════════════════
   ENTITY — Shared base types for entity records
   ═══════════════════════════════════════════════════════════════ */

/** Generic entity record — the universal shape for API data throughout the UI layer. */
export type EntityRecord = Record<string, unknown>;
